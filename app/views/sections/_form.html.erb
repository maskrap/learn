<%= render "layouts/errors", :object => @section %>

<%= form_for [@chapter, @section] do | field | %>
  <%= field.label :name %>
  <%= field.text_field :name %>
  <%= field.label :lesson_id %>
  <%= field.collection_select :lesson_id, Lesson.all, :id, :name %>
  <%= field.submit %>
<% end %>
